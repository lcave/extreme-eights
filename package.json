{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@rails/actioncable": "^7.0.1",
    "esbuild": "^0.14.13",
    "esbuild-rails": "^1.0.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
